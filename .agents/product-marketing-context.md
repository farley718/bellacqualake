# Bell Acqua Lake — Product Marketing Context

**Client:** Bell Acqua Lake
**Last Updated:** 2026-05-05
**Managed by:** Anti-Gravity

---

## Business Overview

Bell Acqua Lake is a water skiing facility/lake offering lessons, ski rides, and memberships.

## Target Audience / ICP

- Historical "Ski Ride With Lesson" clients (20% discount target)
- Old clients (non-ski ride) looking for beginner/intermediate lessons (2-for-1 target)
- New database holders / VIP list (2-for-1 VIP target)

## Unique Selling Proposition (USP)

High-quality water skiing instruction with a new, user-friendly mobile booking portal.

## Goals & Priorities

1. Boost bookings through three targeted marketing campaigns (20% off and 2-for-1 offers).
2. Transition clients to the new booking portal.
3. Increase social media visibility and engagement through community groups and interactive video.

## Brand Voice & Tone

Professional yet accessible and community-focused.

## Key Competitors

Local water sports facilities and ski clubs.

## Current Projects / Active Work

- **Booking Portal Launch:** Updating platform for 2-for-1 logic; mobile UX demonstration.
- **Email Campaigns:** Segmenting lists and drafting three-tier offers (20% off, 2-for-1).
- **Social Strategy:** Weekly reporting alignment and community group engagement.

## Session Notes

### Compacted: 2026-05-11 → 2026-07-23 (6 sessions — spring campaigns, member portal build, 90-day roadmap)
**May 11 (Zoom sync):** planned three campaigns — 20% off for historical ski-ride clients, 2-for-1 for old B/I clients, 2-for-1 VIP for new leads; FB ads payment issue resolved; social reporting moved to weekly snapshots. **Jun 8:** built the full VIP Membership Giveaway campaign (landing + thank-you pages as production HTML, 3 emails with promo code `VIPBONUS`, 2 SMS, GHL webhook wired and tested; assets in `outputs/`). **Jun 24–30 (member portal):** created the missing `member_bookings` table, built the independent member dashboard `bell-acqua-member.html` (members book up to 2 slots/day, access gated by `membership_start`/`membership_end` on profiles), integrated member bookings into the staff dashboard's hourly layout, enforced RLS (anon staff read/cancel member bookings; authenticated members insert/cancel their own), removed the 2-slot minimum, and added staff password reset via the SECURITY DEFINER RPC `admin_update_user_password` gated by a staff PIN (the PIN "1965" noted then is stale — the shared PIN was later '2626', and per-staff PINs replaced it 2026-08-31). Test member account: john.farley.pesigan@gmail.com / password123. Deploys go through `web/netlify-deploy/`. NOTE: the member slot logic built in this era placed members at bottom-of-hour — later found to be built against a broken B/I grid; the corrected model is documented in the 2026-08-22 session. **Jul 18:** B2G1 July promo drafted (2 emails + 2 SMS, code `B2G1JULY2026`, $297 anchor, GHL-paste-ready HTML in `outputs/`) — never went live; still on the backlog. **Jul 23:** client-ready 90-Day Growth Roadmap deck (`outputs/BellAcquaLake_90-Day_Growth_Roadmap.pptx`, 13 slides): targets $25K/mo · 15 members · $15K reserve · $1K/mo ad spend; thesis that demand, not capacity, is the constraint; 13 weeks as monthly sprint boards with MKT/OPS/FIN/OWN owner tags. Key flags: the $1K ad budget must be ring-fenced (H1 2026 leaked to ~$128/mo real spend) and ~$22,400 of in-window renewals beat 15 cold members as the path to the reserve. Week-1 actions (Meta campaign consolidation, renewal-reminder calendar, post-lesson membership follow-up in GHL, "How did you hear about us?" field) remain open on the backlog.
---

### Session Update: 2026-08-12
**Summary:**
Built and shipped the full **affiliate referral program**, live in production. New `bell-acqua-affiliate.html` page (with the real BAL logo): anyone joins with name + email (no password) and gets a unique code (e.g. `JANE-7F2K`), a shareable `?ref=` booking link, and a private token-based dashboard showing referrals and credit coupons. Booking page: Step-1 "Have a referral code?" box auto-fills from `?ref=`, auto-applies **20% off first booking** at the cart via the existing coupon math, and on checkout mints the affiliate a one-time **20% credit coupon** (`CREDIT-XXXXXX`) redeemable in the normal coupon box; confirmation page now invites every new customer to join. Staff dashboard: Referrals tab reworked to list affiliates with active/inactive toggle and per-credit used/available status. All validation is server-side via 5 SECURITY DEFINER RPCs in `web/supabase/migrations/20260812_affiliate_program.sql` (applied by John): first-booking-only, no self-referral, one credit per referred email ever. Tier-ready: `friend_discount_pct` / `reward_pct` are per-affiliate columns — future reward tiers are just an UPDATE. **Netlify CLI was authorized this session — deploy directly with** `npx -y netlify-cli deploy --prod --dir "Marketing Agency\Bell Acqua Lake\web\netlify-deploy" --site 66da87b7-51e4-47db-bbbc-1a47eae71201` (no more manual deploys; a manual "deploy" this session turned out stale). **Next session:** (1) create the GHL "affiliate credit earned" email workflow and paste its webhook URL into `GHL_AFFILIATE_WEBHOOK_URL` in bell-acqua-booking.html — until then affiliates only see credits in their dashboard, no email; (2) announce the program (email blast to existing customers + social); (3) still open from July: B2G1JULY2026 promo go-live in GHL and the 90-day roadmap Week-1 actions.
---

### Session Update: 2026-08-12 (Part 2)
**Summary:**
Wired the GoHighLevel webhook URL (`GHL_AFFILIATE_WEBHOOK_URL`) inside the booking page configuration (`bell-acqua-booking.html` in both `web/` and `netlify-deploy/`) and successfully deployed it live to Netlify production. Sent a test payload to trigger and save the GHL workflow. Drafted an under-the-hood explanation email for Mike and designed a premium HTML referral launch email for existing customers, saved separately as `outputs/referral_promo_email.html`.
---

### Session Update: 2026-08-20
**Summary:**
Massive build day off Mike's two feature requests — all live in production and pushed to GitHub. **(1) Priority features:** cross-calendar time-range blocking (`blocked_ranges` table — staff blocks e.g. "Sep 19, 2 PM–dark" on any mix of B/I, SRL, Member calendars; replaced the broken per-slot picker whose hourly IDs never matched the B/I `:30` grid; member portal now respects blocks); free booking links (staff mints one-time 100% coupon, `?coupon=` URL bypasses 2-session minimum); Copy Reschedule Link on booking cards (manage page auto-verifies via `?ref=&email=`, and the same auto-open link now rides every confirmation email — waiver-page webhook upgraded, lesson page got the confirmed webhook it never had); editable affiliate rewards (per-affiliate + program defaults, RPCs in `20260820_blocking_and_rewards.sql`, migration applied). Also: staff Coupons/Referrals cards → tables, pagination (20/40/100) on Customers/Coupons/Referrals, fixed the dead customer search box, and fixed the manage page's stale top-of-hour reschedule grid. **(2) Membership checkout** (`bell-acqua-membership.html`, linked from booking header): Midweek $1,897 / Unlimited $2,697, add-ons (locker $200, guest passes $300/book, supplementary members $1,897 ea), full contract from Mike's PDF templated in-flow with signature pad (gate code/password redacted from public page; guest passes standardized to $300), payment in full or 4 quarterly installments covering everything (midweek $525-base, unlimited $700-base + extras/4). On payment: `memberships` + `membership_payments` rows, auth signup, `membership_activate` RPC provisions the profile — member portal works immediately; midweek members restricted to Mon–Thu booking. **Auto-charge is fully live:** `membership-checkout` edge function saves cards (Stripe Customer + setup_future_usage), `charge-membership-installments` charges due installments daily via pg_cron (`bal-charge-installments`, 15:00 UTC, verified active) — both deployed via John's browser, CRON_SECRET set, all migrations applied (`20260820_membership_checkout.sql`). Staff Members tab: plan badges + Membership Purchases table with per-installment mark-paid. **Open:** GHL membership welcome workflow — email template ready at `outputs/membership_welcome_email.html`, paste its webhook URL into `GHL_MEMBERSHIP_WEBHOOK_URL` in bell-acqua-membership.html; a stray Stripe test customer (autocharge-test@example.com) can be deleted; still pending from before: B2G1JULY2026 promo go-live, affiliate program announcement, 90-day roadmap Week-1 actions. Bigger-app plan with 7 questions for Mike: `outputs/membership-app-implementation-plan.md`. Flagged: member calendar and B/I calendar don't cross-check bookings (double-booking risk).
---


### Session Update: 2026-08-22
**Summary:**
Two production fixes to the booking system, both live and pushed. **(1) B/I calendar restored to top-of-hour.** Traced a real double-booking risk to its root: the Beginner/Intermediate booking page had been silently switched from top-of-hour (7:00-7:30) to bottom-of-hour (7:30-8:00) on 2026-06-23 at 23:51 UTC (Netlify deploy `6a3b1c0d`, no commit, no session note) — putting public customers into members-only time and killing the B/I<->SRL cross-blocking, which keys on slot ids ending `00`. Confirmed original design via 84 pre-flip bookings on `HH00`, the SRL page and staff dashboard (both never changed), and Mike's own description. Restored in `bell-acqua-booking.html` and `bell-acqua-manage.html`; cross-blocking revived with no new code (verified live: Andreas Thoma's Aug 23 lessons now close 9/10/11 AM to B/I). **Correct model: top of hour = B/I (30 min) + SRL (2×15 min), shared and cross-blocked; bottom of hour = members only, 2×15 min.** The 14 upcoming B/I bookings taken on the wrong grid (Leslee Bender, Tara Coronado, Mike Riley, Ryan Sydenham ×6, Misty Allard ×2, Mike Todd ×2) were left untouched per John — `20260822_grandfather_bottom_of_hour_bi.sql` blocks those windows on the member calendar instead (applied). Email drafted for Mike at `outputs/mike-booking-grid-email.md` — **NOT yet sent**, awaiting John. **(2) Membership waiver gate built.** Membership checkout was writing `status='active'` and activating the portal immediately with no liability waiver anywhere — the checkout signature is the membership *agreement* (fees/guest rules/payment terms) and contains zero release-of-liability language; supplementary members got no account, no waiver, no record. Now checkout saves the membership as `pending` and creates one `waiver_requests` row per skier (primary + each supplementary); the portal stays locked until all are signed, then the waiver page calls the new `membership_waiver_check()` RPC which flips it to `active` and provisions the profile. Also closed a related hole: the portal treated a missing profile as "no date restriction" instead of "no access", and the `members_insert_own` RLS policy only checked `auth.uid()`, so the lock would have been cosmetic — the policy now requires a profile whose membership window covers the booking date. Migration `20260822_membership_waivers.sql` applied. Fully tested (1-of-2-signed correctly keeps the membership locked; active and midweek members unaffected).
**Next session:** (1) **Two test rows still need deleting** — anon has no DELETE policy so my cleanup silently no-op'd: `DELETE FROM waiver_requests WHERE membership_id='BAM-VERIFY01'; DELETE FROM memberships WHERE id='BAM-VERIFY01';` (2) **`GHL_MEMBERSHIP_WEBHOOK_URL` is still the placeholder `YOUR_MEMBERSHIP_WEBHOOK_URL`** — until John creates the GHL workflow and provides the URL, no waiver email and no welcome email fire; members only get waiver links on the confirmation screen and must forward supplementary ones by hand. Payload already carries `waiver_url`, `waivers[]`, `supplementary_waivers`, `membership_status` — one-line change once the URL arrives. (3) Send the Mike email. (4) Consider renaming `memberships.waiver_*` columns (they hold the contract signature, not a waiver) — deferred because it would break the staff dashboard and installment charger. (5) Still pending from before: B2G1JULY2026 promo go-live, affiliate program announcement, 90-day roadmap Week-1 actions.
---

### Session Update: 2026-08-26
**Summary:**
Built a 40-school youth ski club outreach & enrichment tracker (xlsx, delivered to John) covering three segments: Tier 1 Roseville-area RJUHSD high schools (7), Tier 2 wider Sacramento-region public districts (21 — Sacramento City, Elk Grove, Natomas, San Juan, Folsom Cordova), and Private & Religious Schools (12). Each row has address, phone, website, a named PE/athletics contact where publicly available (front-office fallback flagged amber otherwise), and an Outreach Status tracker. While researching, discovered the existing "bell_acqua_enriched" Google Sheet in Drive (built 2026-07-31, private schools only) and merged it in as the third tab instead of creating a disconnected duplicate — corrected two stale Athletic Director contacts found there (Christian Brothers HS: Melissa Flowers, not Dale Milton who is Asst. AD; Sacramento Adventist Academy: Cyrus Jones, verified via the athletics program's own site) and completed the blank St. Ignatius Parish School row (K-8 only, no HS athletics).
**Next session:** Push the workbook into Google Sheets in the Bell Acqua Lake Drive folder if John wants it live there (offered, not yet confirmed); once John reviews, begin actual outreach starting with Tier 1 (Granite Bay and Woodcreek flagged as strongest first contacts — named AD + large campus). Still open from before: two test DB rows to delete, `GHL_MEMBERSHIP_WEBHOOK_URL` placeholder, the Mike booking-grid email, B2G1JULY2026 promo go-live, affiliate program announcement, 90-day roadmap Week-1 actions.
---

### Session Update: 2026-08-28
**Summary:**
Reviewed the new **Youth Ski Club** landing page (`https://link.fgfunnels.com/preview/neQxlvj8lQmFl0NFzxeV`) and built the enrollment email sequence for it. Offer on the page: 3-event waterskiing (slalom/trick/jump) for ages 10-17, 4 coached sessions a week (Tue/Thu or Mon/Wed, 4-6:30pm), fall season Sept 15 - Oct 31, **$2,000 all-inclusive**, weekend lessons extra; page collects parent name, skier name, skier age, phone, preferred schedule, email. Wrote a **5-email welcome/close sequence** for form-fill leads who have not paid: (1) Day 0 spot-held confirmation, (2) Day 1 what-a-session-looks-like, (3) Day 3 five parent objections, (4) Day 5 what $2,000 buys (up to 28 coached sessions), (5) Day 7 last call before Sept 15. Delivered as **five standalone GHL-paste-ready HTML files** (table layout, fully inline styles, hidden preheader per email, `{{contact.first_name}}` merge fields) in `outputs/youth-ski-club-emails/` plus `README-sequence.md` with the subject/preview/timing table; the original all-in-one preview file is `outputs/youth-ski-club-welcome-sequence.html`. **All CTA buttons point to `https://ski.bellacqualake.com/youth-ski-club_checkout`** (John supplied; not yet verified as loading). Also proposed 3 subject-line variants per email in chat (not yet written into the files).
**Next session:** (1) Load the 5 emails into a GHL workflow triggered by the Youth Ski Club form, with an exit condition on payment received; (2) verify the checkout URL resolves; (3) optionally write the A/B subject-line alternates into README-sequence.md; (4) **landing page gap flagged: zero social proof** — no parent testimonial, no photo of kids skiing, no coach bio on a $2,000 ask for someone's child; a proof block is the highest-value next edit. Ties to the 2026-08-26 40-school outreach tracker — this sequence is the back half of that funnel. Still open from before: two `BAM-VERIFY01` test DB rows to delete, `GHL_MEMBERSHIP_WEBHOOK_URL` placeholder, the Mike booking-grid email, B2G1JULY2026 promo go-live, affiliate program announcement, 90-day roadmap Week-1 actions.
---

### Session Update: 2026-08-29
**Summary:**
Wired the new **Youth Ski Club** landing page (Fall 2026 season, Sept 15 – Oct 31, $2,000 all-inclusive, ages 10–17) to a GoHighLevel inbound webhook and drafted the conversion layer around it. **(1) Webhook live and tested:** URL `https://services.leadconnectorhq.com/hooks/rqN9GeQaEfpisadTVaUO/webhook-trigger/5567acbc-46db-423e-b9bc-aa1ce705cd5d` — two test payloads fired, both HTTP 200 `{"status":"Success: test request received"}`. **Important environment note: `services.leadconnectorhq.com` is blocked by the org egress allowlist from BOTH the cloud container and the device shell — the only way to hit GHL webhooks from a Cowork session is through Claude in Chrome (`javascript_tool` fetch from a leadconnectorhq tab).** The form now posts 14 fields: `first_name, last_name, name, email, phone` (standard GHL contact) + `parent_name, skier_name, skier_age, preferred_schedule` (custom) + `program, season, price, source, page_url, submitted_at` (attribution). `parent_name` is split into first/last so GHL builds a real contact. JS uses a normal JSON fetch with a `no-cors` text/plain fallback so a CORS block still delivers the lead. Saved as `outputs/youth-ski-club-webhook-script.html` (drop-in `<script>` replacement) and `outputs/youth-ski-club-landing.html` (full page rebuild). **(2) Thank-you page copy rewritten around a "skip the line" angle** (`outputs/youth-ski-club-thankyou-copy.md`): the old "You're In! / Next Steps for Parents" layout tells the parent they're done and then hands them homework, which kills same-session payment — replaced with a hero reframe (request = in line, payment = spot on the water), a scarcity band with per-cohort counts, the $2,000 checkout as the page centerpiece, and the three next-step cards demoted below it as post-payment tasks. **(3) Also now in `outputs/`:** a 5-email pre-payment nurture sequence for unpaid form submitters (`youth-ski-club-emails/`, CTAs pointing at `ski.bellacqualake.com/youth-ski-club_checkout`) and `youth-ski-club-welcome-sequence.html`.
**Open / next session:** (1) **`youth-ski-club-landing.html` is incomplete — the logo `<img src>` is still the literal string `LOGO_DATA_URI_PLACEHOLDER`**; the base64 BAL logo must be pasted back in before that file is usable (the drop-in script file is unaffected and is the safer path anyway). (2) **Phone number conflict:** the thank-you mockup shows (916) 991-5341, the landing page footer and site use (916) 919-5726 — confirm before anything ships. (3) Build the $2,000 checkout; the membership checkout's Stripe customer + 4-installment plumbing can carry it if Mike approves a payment plan. (4) Cohort "spots left" counts are hardcoded — wire to real registrations or have Mike update weekly. (5) Build the GHL workflow behind the trigger (create/update contact → tag `youth-ski-club-fall-2026` → parent confirmation → internal notify Mike). (6) Still open from before: two `BAM-VERIFY01` test rows to delete, `GHL_MEMBERSHIP_WEBHOOK_URL` placeholder, the Mike booking-grid email, school outreach workbook → Drive + Tier 1 outreach, B2G1JULY2026 go-live, affiliate announcement, 90-day roadmap Week-1 actions.
---

### Session Update: 2026-08-29 (Part 2)
**Summary:**
Rebuilt the Youth Ski Club thank-you page and built the real Stripe checkout behind it. **(1) Found the funnel's actual leak:** the live checkout at `ski.bellacqualake.com/youth-ski-club_checkout` **cannot take a payment** — zero inputs, zero buttons, and it iframes `youth-ski-club_checkout-page`, which iframes *itself*. Every "Secure Limited Spots Today!" button on the live thank-you page dead-ends there. **(2) Thank-you page v2** (`outputs/youth-ski-club-thankyou-v2.html`, paste-into-GHL custom code): hero kept, plus "a member of our team will be reaching out to you very soon"; "Next Steps for Parents" replaced by a gold urgency card ("Limited slots this fall" / "Claim your skier's spot"); the empty checkout embed removed entirely and replaced with a value stack + two **"Complete Your Registration"** CTAs; the three next-step cards demoted below as "Once you're registered". Both CTAs read one `CHECKOUT_URL` variable at the bottom of the file — **still `https://REPLACE-ME.netlify.app/youth-ski-club-checkout`, set it once the checkout is deployed.** John's call: keep the price at $2,000 (no discount) — urgency comes from real capacity only. Invented "4/6 spots left" counters were stripped; scarcity claims must come from real data. **(3) Stripe checkout built** on the membership-checkout pattern: `web/bell-acqua-youth-ski-club-checkout.html` (+ copy in `netlify-deploy/`) and migration `web/supabase/migrations/20260829_youth_ski_club.sql`. Three steps (parent & skier details incl. skill level, emergency contact and medical notes → cohort → Stripe card). Reuses the **already-deployed** `membership-checkout` edge function with the `create-payment-intent` fallback — no new function, no new secrets — so the card is saved on a Stripe Customer and a future installment option needs no new plumbing. New tables `ysc_registrations` + `ysc_cohort_seats`, view `ysc_cohort_availability`, and SECURITY DEFINER `ysc_seats_remaining()`; **seat counts are read live from the DB and re-checked immediately before charging**, so a cohort that fills mid-checkout bounces the parent back to step 2 before any money moves, and an unavailable count says "Limited slots remaining" rather than guessing. RLS: anon may INSERT a registration but **cannot read the roster** (parent emails, kids' ages, medical notes). The row write is failure-swallowing by design — a paying parent must never see "card failed" because an insert hiccuped. On success it POSTs to the same Youth Ski Club GHL webhook carrying `registration_id` / `registration_status:paid` / `amount_paid` / `stripe_payment_intent`, so one workflow serves both the unpaid lead and the paid registration (branch on `registration_status`).
**Next session (moving to a LOCAL session so deploys can run — cloud Cowork has no network for netlify/git push):** (1) Run `20260829_youth_ski_club.sql` in the Supabase SQL editor **with the real cohort caps** — 12/12 is a placeholder. (2) Deploy: `npx -y netlify-cli deploy --prod --dir "Marketing Agency\Bell Acqua Lake\web\netlify-deploy" --site 66da87b7-51e4-47db-bbbc-1a47eae71201`. (3) Put the deployed URL into `CHECKOUT_URL` in the thank-you page and paste that page into GHL. (4) **Test with a real card and refund it — the Stripe key on the page is LIVE.** (5) Build the GHL workflow behind the webhook (contact + tag `youth-ski-club-fall-2026`, receipt/welcome guide, internal notify Mike). (6) Offered, not built: tie the waiver to the registration via `waiver_requests` + a `ysc_waiver_check()` RPC, mirroring the membership waiver gate, so it's visible which skiers still can't ski on Sept 15. (7) Decide whether the three Google Drive video embeds return below the program summary. (8) Still open from before: two `BAM-VERIFY01` test rows, `GHL_MEMBERSHIP_WEBHOOK_URL` placeholder, the Mike booking-grid email, school outreach workbook → Drive + Tier 1, B2G1JULY2026 go-live, affiliate announcement, 90-day roadmap Week-1 actions.
---

### Session Update: 2026-08-31 (covers 08-30 → 08-31)
**Summary:**
**Youth Ski Club checkout is LIVE and fully verified** at `https://meek-duckanoo-53e84b.netlify.app/bell-acqua-youth-ski-club-checkout.html` — this URL goes into the thank-you page's "Complete Your Registration" button (already written into `outputs/youth-ski-club-thankyou-v2.html`, which is GHL-paste-ready and also got the phone fix). All three YSC migrations applied by John: `20260829_youth_ski_club.sql` (tables/seat RPC — **cohort caps still the 12/12 placeholders**, change via `UPDATE ysc_cohort_seats SET seats=N WHERE cohort='tue_thu'|'mon_wed'`), `20260830_ysc_waiver_columns.sql`, `20260831_waiver_archive.sql`. Highlights: **(1) Waiver moved INTO the checkout** — now 4 steps (Parent & Skier → Cohort → Safety Waiver → Payment): verbatim facility waiver text (sections A–E + natural-guardian clause), per-section agree boxes, photo release, printed name + drawn signature, all stored on the registration row (`waiver_signer`/`waiver_signature`/`photo_consent`; insert falls back without those columns rather than ever losing a paid row). The dead post-payment "Sign the Safety Waiver" button (pointed at nonexistent bellacqualake.com/waiver) is gone. Also fixed mobile: checkbox blowup from global input CSS, and sticky Program Summary overlaying the single-column form (cascade-ordering gotcha — the override must come AFTER the base `.summary` rule). Phone corrected to (916) 919-5726 everywhere. **(2) Test modes on the checkout:** `?test=1` = dry run (status pending, $0, no seat, webhook `registration_status:'test'`); `?test=paid` = full sale simulation, no card (status **paid**, $2,000, consumes seat, webhook `'paid'`) — John ran the paid sim successfully and then the cleanup `DELETE FROM ysc_registrations WHERE id LIKE 'YSC-TEST-%'` (DB verified clean, seats back to 12/12). Real-card test never done (John has no card available) — go-live decision rests on the simulation. **(3) Staff dashboard "Waivers" tab (Waiver Archive)** — unified booking + membership (`waiver_requests`, anon-read, PDF via existing `?token=&view=pdf`) + YSC waivers (PIN-gated `staff_ysc_waivers`/`staff_ysc_waiver_pdf` RPCs; **PIN '2626' hardcoded in BOTH the RPCs and staff CONFIG — change together**; medical notes never exposed). Columns: person, type, signed date, 1-yr validity badge, member status, level, last skied; search; 20/40/100 pagination; per-row PDF (YSC PDFs via new `?ysc=<id>&pin=&view=pdf` mode on bell-acqua-waiver.html reusing generateWaiverPDF). Verified live: 134 waivers, 107 with PDFs, YSC rows + PDF download tested end-to-end. **(4) Post-checkout emails** in `outputs/youth-ski-club-checkout-emails/`: `01-payment-confirmed.html` (paid + waiver_signed=true) and `02-payment-received-waiver-pending.html` (paid + waiver missing — edge path; deliberately NO waiver link since none is self-serve — pair with internal notify so staff mint one), plus README with branch logic and merge-field mapping (`skier_name`/`preferred_schedule`/`registration_id` must be mapped as GHL custom fields). **(5) Full sample paid payload fired at the YSC webhook** (accepted, event id SORDabr8PLT0VfF1reer) so GHL field-mapping can be configured; sample contact jane.sample@example.com may need deleting in GHL. Webhook (both landing form + checkout): `https://services.leadconnectorhq.com/hooks/rqN9GeQaEfpisadTVaUO/webhook-trigger/5567acbc-46db-423e-b9bc-aa1ce705cd5d`. All work committed and pushed to the bellacqualake repo through `c536b01`.
**Next session:** (1) John: set real cohort caps, paste thank-you v2 into GHL, build the GHL workflow (branch on `registration_status`: paid → email 01/02 by `waiver_signed`, test → internal only, absent → 5-email nurture; internal notify Mike; tag `youth-ski-club-fall-2026`). (2) Confirm with Mike whether the go-live requires a real-card test or the paid simulation suffices. (3) Pre-existing staff-dashboard console 400s (x2, not from this session's code) worth a look. (4) Offered: tokenized self-serve YSC waiver flow if waiver-pending registrations become common; customer copy of signed waiver (PDF download on success screen) designed but not built. (5) Still open: `BAM-VERIFY01` test rows, `GHL_MEMBERSHIP_WEBHOOK_URL` placeholder, Mike booking-grid email, school outreach → Drive + Tier 1, B2G1JULY2026 go-live, affiliate announcement, roadmap Week-1 actions.
---

### Session Update: 2026-08-31 (Part 2)
**Summary:**
**Per-staff logins, audit trail, and roster-driven booking notifications — built, deployed, and verified working end-to-end.** Migration `20260831_staff_accounts.sql` (+ required fix `20260831_staff_accounts_fix.sql` — **Supabase gotcha: pgcrypto lives in the `extensions` schema, so any function using `crypt()`/`gen_salt()` must SET `search_path = public, extensions`**, plain `public` hides them) creates `staff_members` (personal 4–8-digit PINs bcrypt-hashed, `admin`/`coach` roles, `notify_new_bookings` toggle), `staff_sessions` (PIN login mints a 30-day token), `staff_audit_log`, and RPCs (`staff_login/session_info/log/list/save/set_pin/audit`, `booking_notify_recipients`); the YSC waiver RPCs now accept a session token OR the legacy '2626'. **Dashboard** (bell-acqua-staff.html): same PIN screen but the PIN typed determines identity (header shows 👤 name·role + Log out; legacy fallback if migration missing); new **🧑‍🏫 Staff tab** (admin adds/edits staff, sets PINs — unique, server-checked — toggles 🔔/active; coach read-only) and **🕘 Activity tab** (searchable, paginated audit log). **18 mutation points log who-did-what**: coupons create/toggle/delete, free links, staff bookings, cancellations, date/slot/range blocks+unblocks, member add/dates/password-reset, installments, affiliate toggle/rewards/defaults; staff mgmt + logins are audited server-side (can't be skipped). **Notifications**: booking + lesson pages fetch `booking_notify_recipients()` at confirm time and put `staff_notify_emails`/`staff_notify_phones` (comma-separated) into the booking-confirmed GHL payload; John repointed the GHL notification step at `{{inbound_webhook.staff_notify_emails}}` and published; live-roster test payload (`BAL-TESTNOTIF2`) delivered to BOTH roster emails (mtbalake@ + john.farley.pesigan@) — **roster changes now drive notifications with zero GHL edits**. John created real staff accounts. Bootstrap account "Shared Legacy Admin" (PIN 2626) exists; actions under it are logged but anonymous. Six `.mov` testimonial videos landed in `Ad Creatives/` + `web/netlify-deploy/ad-creatives/` mid-session; John chose to KEEP them — committed (one is 54MB, GitHub warns) and now live on Netlify. All pushed through `2aeb601`.
**Next session:** (1) John: deactivate "Shared Legacy Admin" once all real staff have accounts (verify he did). (2) Delete GHL test contacts: Test Mapping, Roster Test, Jane Sample. (3) Caveats on record: actions under the pre-migration shared PIN were never logged; `booking_notify_recipients()` is anon-callable by design (public booking pages need it) — move to an edge function if exposing subscribed staff emails/phones ever becomes a concern; GHL custom-phone field may not fan out the comma-separated `staff_notify_phones` for SMS (email confirmed working, SMS untested). (4) Everything from Part 1 above: YSC cohort caps, thank-you v2 into GHL, YSC GHL workflow branches, staff-dashboard 400s, and the long-running backlog.
---
